# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.eggs/
.pytest_cache/

# Trained artifacts + datasets — NEVER committed. The model bundle is NOT
# shipped in the wheel; it's a GitHub release asset that the package fetches
# on first use and caches per-user. Datasets are derived from private sources
# and must not leak; local training/export outputs live under models/.
data/
models/
*.joblib
*.json.gz

# Local analysis reports — methodology/eval detail, keep local-only
RETRAIN_REPORT.md
NN_VS_LOGREG.md
